{
  "name": "delta-keys",
  "version": "1.0.0",
  "private": true,
  "description": "DELTA.KEYS digital key store with customer storefront and admin key inventory.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
